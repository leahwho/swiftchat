import 'package:flutter/material.dart';

class QuickBoard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.white,
      ),
      body: Text('This is going to be the quick board page'),
    );
  }
}

// TODO 1: get buttons on this page with some placeholder text